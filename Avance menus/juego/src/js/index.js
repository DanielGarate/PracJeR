import { Game } from 'game.js';
import { Menu } from 'menu.js';


const config = {
  type: Phaser.AUTO,
  width: 800,
  height: 600,
  scene: [Game],
}
var game = new Phaser.Game(config);

